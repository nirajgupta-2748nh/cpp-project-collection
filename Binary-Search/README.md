# Binary Search in C++

A simple C++ program that implements the **Binary Search algorithm** to find an element in an array.

## Features

* Accepts user input for array elements
* Searches element using binary search
* Returns index if found, otherwise `-1`

## Concepts Used

Arrays, functions, loops, conditionals

## Usage

Compile and run the program, enter sorted array values, and provide the key to search.

---